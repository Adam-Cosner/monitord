mod cpu;
mod disk;
mod gpu;
mod mem;
mod net;
mod proc;

pub struct MetricManager {
    // instances of each metric collector
}

impl MetricManager {
    pub fn new() -> Self {
        MetricManager {}
    }
}
