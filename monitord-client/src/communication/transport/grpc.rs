use crate::communication::transport::Transport;
use crate::error::CommunicationError;

pub(crate) enum GrpcConfig {
    // todo
}

pub struct GrpcTransport {
    // todo
}

impl GrpcTransport {
    pub fn new() -> Result<Self, CommunicationError> {
        todo!()
    }
}

impl Transport for GrpcTransport {

}